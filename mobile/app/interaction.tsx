import React from "react";
import { SafeAreaView, SafeAreaProvider } from "react-native-safe-area-context";
import Interaction from '../features/game/Interactions';


export default function Scene() {
  return (
    <Interaction />
  );
}
